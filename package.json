{
  "dependencies": {
    "three": "^0.153.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^4.5.3"
  },
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
